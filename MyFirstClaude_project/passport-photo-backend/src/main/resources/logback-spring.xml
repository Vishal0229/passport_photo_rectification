<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Console only — Railway captures all stdout/stderr automatically -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %-5level [%15.15t] %-40.40logger{39} : %msg%n</pattern>
        </encoder>
    </appender>

    <logger name="com.passport.photo" level="INFO" additivity="false">
        <appender-ref ref="CONSOLE"/>
    </logger>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

</configuration>
